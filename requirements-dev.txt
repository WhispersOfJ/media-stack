# Test-only dependencies, on top of control-panel/requirements.txt.
# Install both before running pytest:
#   pip install -r control-panel/requirements.txt -r requirements-dev.txt
pytest==9.1.1
plexapi==4.18.2
requests==2.34.2
beautifulsoup4==4.15.0
# starlette.testclient (used by fastapi.testclient.TestClient, imported by
# control-panel's own test fixtures) prefers this over plain httpx now -
# without it, every test run emits a StarletteDeprecationWarning.
httpx2==2.9.1
