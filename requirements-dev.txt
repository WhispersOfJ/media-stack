# Test-only dependencies, on top of control-panel/requirements.txt.
# Install both before running pytest:
#   pip install -r control-panel/requirements.txt -r requirements-dev.txt
pytest==9.1.1
plexapi==4.18.2
requests==2.34.2
beautifulsoup4==4.15.0
