# Test-only dependencies for the scripts/fish/systemd suite. scripts/*.py are
# stdlib-only, so they have no requirements file of their own; the Django
# backend's deps live in control-panel-django/requirements.txt.
#   pip install -r requirements-dev.txt
pytest==9.1.1
plexapi==4.18.2
requests==2.34.2
beautifulsoup4==4.15.0
httpx2==2.9.1
