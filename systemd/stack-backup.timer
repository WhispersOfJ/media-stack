[Unit]
Description=Daily stack config backup (restic)

[Timer]
# Before Watchtower's 4am image updates, so a bad update never rolls in
# ahead of that day's config backup.
OnCalendar=*-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
