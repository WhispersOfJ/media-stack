[Unit]
Description=Daily Radarr/Sonarr native backup

[Timer]
# After stack-backup's 03:30 config snapshot, before Watchtower's 04:00
# image updates - same reasoning as stack-backup.timer's own placement.
OnCalendar=*-*-* 03:40:00
Persistent=true

[Install]
WantedBy=timers.target
