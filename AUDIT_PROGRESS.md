# Full-Stack Audit Progress

Resumable checklist for the correctness/connectivity/observability audit. See plan: `/home/bear/.claude/plans/goofy-scribbling-sunrise.md`.
Companion doc: `AUDIT_FINDINGS.md` (architectural issues flagged but not fixed).

| # | Directory | Status | Issues Found | Issues Fixed | Tests Added |
|---|-----------|--------|---------------|----------------|-------------|
| 1 | config/ + .env.example + docker-compose.yml | done | 4 dangling env vars in control-panel's compose block (RADARR_ANIME_API_KEY, SONARR_ANIME_API_KEY, SPEEDTEST_TRACKER_API_TOKEN, ORGANIZR_API_KEY) injected for routers deleted in c7fc6b8; 12 matching stale vars in .env.example (incl. RADARR/SONARR_ANIME_URL, ORGANIZR_*, SPEEDTEST_TRACKER_*) | all 4 dangling compose env injections removed; .env.example pruned to match; live .env already clean (no action needed there) | 0 (no functions in this layer, config/env only) |
| 2 | control-panel/core/ | done | zero test coverage on 11 of 12 modules (only import_starvation.py had tests, since the app.py-era test_helpers.py and anime-only test_arr_client_apps.py were both correctly deleted in c7fc6b8 but the underlying logic's tests were never re-ported to the new module paths); no shared logger anywhere in the stack; DEDUP_SUFFIX_RE year/dedup-suffix ambiguity (flagged, not fixed); main.py docstring stale (flagged, not fixed) | added core/logging_config.py (shared `control-panel` logger, lazy file handler, /logs bind-mount + .gitignore entry added); wired into core/responses.py's fail() so every existing fail() call site now logs at ERROR for free | 108 (test_arr_client_helpers.py, test_docker_client.py, test_security.py, test_settings.py, test_api_hit_counts.py, test_nzbdav_client.py, test_plex_client.py, test_responses_and_host_paths.py, test_host_helper_client_and_db.py, test_logging_config.py) |
| 3 | control-panel/services/ + main.py + models/ | done | 5 routers with zero test coverage (prowlarr, queue, radarr, ratings, sonarr); 2 mutating routes (gaps2_scan, gaps2_push, plexanisync_run_now) using current_user_or_service with no justification comment (real convention violation, confirmed legitimate via fish CLI callers); 2 silent except-Exception swallows in plex/router.py; 1 stale cross-reference comment (kometa); AuditLog write-only (flagged); models/ - all 9 confirmed wired, no orphans | added justification comments to the 3 undocumented mutating routes; added logger.error() calls to both silent excepts in plex/router.py; fixed stale kometa cross-reference comment | 35 (test_prowlarr_router.py, test_queue_router.py, test_radarr_router.py, test_ratings_router.py, test_sonarr_router.py) |
| 4 | control-panel/static/ | done | palette.js and auth.js structurally untestable under Node (module-level document.getElementById calls block import entirely); all 116 commands.json PathTemplates verified to resolve to real backend routes (via openapi schema, not naive string match — 7 initial false positives all explained); no dangling references to removed services in static/ | none needed (frontend/backend connectivity confirmed clean) | 4 new (core.test.js, covering formatLogLine/formatLogText); 4 pre-existing (fleet.test.js, sparkline.test.js) — 8 total JS tests, all passing via `node --test` |
| 5 | scripts/ | done | 4 of 24 files had no test coverage (mdblist_toplists_import.py, provision-cleanuparr-instances.py, notify-discord.sh, check-container-health.sh) — plan assumed only 1; the 2 .sh files match the repo's existing convention of no bash unit tests, reviewed by reading instead; provision-cleanuparr-instances.py's provision_seeker() had an unguarded request() call (no try/except, unlike every other call in the file); its main() exit code doesn't reflect a seeker-fetch failure (flagged, not fixed); no dangling references to removed services, no undocumented env vars | added try/except around provision_seeker()'s GET call, now prints FAILED to stderr and returns [] instead of crashing unhandled | 23 (test_mdblist_toplists_import.py: 14, test_provision_cleanuparr_instances.py: 9) |
| 6 | systemd/ + Dockerfile + entrypoint.sh | done | media-stack.service and entrypoint.sh both referenced a `--profile extras` compose profile that was never defined (harmless today, landmine for a future real profile); control-panel/Dockerfile (a 2nd Dockerfile, missed on first pass) had a build-breaking `COPY app.py ...` for a file deleted in this session's consolidation — caught only during live verification when `docker compose build` failed outright; health-monitor's SCHEDULED_JOBS only covers 1 of 9 real timers (flagged, not fixed, out of scope) | removed `--profile extras` from media-stack.service + entrypoint.sh; fixed control-panel/Dockerfile's dangling app.py COPY + stale comment, rebuilt image, confirmed container boots clean and stays healthy | 9 (test_systemd_units.py: cross-reference gate tests for the whole deploy layer, since no Python functions exist in this directory to unit-test) |

Out of scope (flagged, not audited): `fish-functions/` (thin CLI glue, no branching logic), `TUI/` (empty), `media/`/`backups/`/`data/` (data, not code).

Baseline: 721 tests passing as of commit c7fc6b8.
